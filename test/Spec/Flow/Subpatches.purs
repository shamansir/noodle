module Noodle.Test.Spec.Flow.Subpatches
    ( spec
    ) where


import Prelude (Unit, ($))

import Test.Spec (Spec, describe, pending)

spec :: Spec Unit
spec = do

  describe "supbatches node" $ do

      pending "subpatches work"
